diff -Nuar violetland-v0.2.3.orig/CMakeLists.txt violetland-v0.2.3.new/CMakeLists.txt
--- violetland-v0.2.3.orig/CMakeLists.txt	2009-11-19 21:57:06.000000000 +0500
+++ violetland-v0.2.3.new/CMakeLists.txt	2009-11-19 23:43:24.000000000 +0500
@@ -54,12 +54,17 @@
 )
 
 add_definitions(-DINSTALL_PREFIX="${CMAKE_INSTALL_PREFIX}")
+add_definitions(-DDATA_INSTALL_DIR="${DATA_INSTALL_DIR}")
 
 add_executable(violetland ${srcFileList})
 
 target_link_libraries(violetland ${libList})
 
-set(relResPath share/violetland)
+if(DEFINED DATA_INSTALL_DIR)
+	set(relResPath ${DATA_INSTALL_DIR}/violetland/)
+else(DEFINED DATA_INSTALL_DIR)
+	set(relResPath share/violetland)
+endif(DEFINED DATA_INSTALL_DIR)
 
 install(TARGETS violetland DESTINATION bin)
 install(FILES
diff -Nuar violetland-v0.2.3.orig/src/system/utility/FileUtility.cpp violetland-v0.2.3.new/src/system/utility/FileUtility.cpp
--- violetland-v0.2.3.orig/src/system/utility/FileUtility.cpp	2009-11-19 21:57:06.000000000 +0500
+++ violetland-v0.2.3.new/src/system/utility/FileUtility.cpp	2009-11-19 23:44:29.000000000 +0500
@@ -61,19 +61,24 @@
 #endif //_WIN32
 #if defined linux || defined __FreeBSD__
 	char result[PATH_MAX];
-	if (readlink("/proc/self/exe", result, PATH_MAX) != -1) {
-		FileUtility::truncateFullPathToDir(result);
-		m_appPath = result;
-		m_resPath = m_appPath;
-		m_resPath.append("/../share/violetland/");
-	} else {
+//	if (readlink("/proc/self/exe", result, PATH_MAX) != -1) {
+//		FileUtility::truncateFullPathToDir(result);
+//		m_appPath = result;
+//		m_resPath = m_appPath;
+//		m_resPath.append("/../share/violetland/");
+//	} else {
 #ifndef INSTALL_PREFIX
 #define INSTALL_PREFIX "/usr/local";
 #endif //INSTALL_PREFIX
-		m_appPath = m_resPath = INSTALL_PREFIX;
-		m_appPath.append("/bin");
-		m_resPath.append("/share/violetland/");
-	}
+        m_appPath = INSTALL_PREFIX;
+        m_appPath.append("/bin");
+
+#ifndef DATA_INSTALL_DIR
+#define DATA_INSTALL_DIR "/usr/local/share/games";
+#endif //DATA_INSTALL_DIR
+	m_resPath = DATA_INSTALL_DIR;
+	m_resPath.append("/violetland/");
+//	}
 	m_usrPath = getenv("HOME");
 	m_usrPath.append("/.config");
 	mkdir(m_usrPath.c_str(), S_IRWXU | S_IRGRP | S_IROTH);
